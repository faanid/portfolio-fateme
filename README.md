# portfolio-fateme

Portfolio content (projects, skills, work experience) is static data in `data/`.
The contact form, project-submission form, and project-comment form send email
via SMTP instead of writing to a database. Copy `.env.example` to `.env.local`
and fill in your SMTP credentials before running the forms locally.

